{
  "name": "nexgen-canvas-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "gen:lti-keys": "node scripts/generate-lti-jwk.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "jose": "^5.10.0"
  }
}
